#ifndef ENGINE_HELPER_H
#define EVGINE_HELPER_H

#include <cstdint>
#include <vector>
#include "eventhandler.h"

using namespace std;
std::vector<uint64_t> bitBreaker(uint64_t bitboard);
std::vector<uint64_t*> allMoves(uint64_t* board,bool turn)

#endif
